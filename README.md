# HaltProof

HaltProof is an emergency-shutdown orchestration and cryptographic
audit-proof layer for compute clusters. It coordinates already-trusted
cluster primitives (Slurm, Kubernetes, IPMI/BMC) and produces a signed,
tamper-evident record of every shutdown action for incident-response and
compliance reporting.

Full documentation is in progress.
